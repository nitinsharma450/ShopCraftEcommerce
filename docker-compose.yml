

services:
  mysql:
    image: mysql:8
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ecommerce_backend
    ports:
      - "9999:9999"
    depends_on:
      - mysql
    restart: always

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    container_name: ecommerce_admin
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always

  userportal:
    build:
      context: ./website
      dockerfile: Dockerfile
    container_name: ecommerce_user_portal
    ports:
      - "5174:5174"
    depends_on:
      - backend
    restart: always

volumes:
  mysql_data:
